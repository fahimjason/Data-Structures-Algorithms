function compressBoxesTwice(boxes, boxes2) {
    boxes.forEach(element => {
        console.log(element);
    });

    boxes2.forEach(element => {
        console.log(element);
    });
}

// BIG O(a + b)